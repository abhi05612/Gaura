import { container, section } from "@/lib/constants";

export default function Navbar() {
  return (
    <section className={section}>
      <div className={container}>
        {/* Navbar — coming soon */}
      </div>
    </section>
  );
}
